import type { Metadata } from "next";
import { Geist, Geist_Mono, Jua, Nanum_Pen_Script, Noto_Sans_KR } from "next/font/google";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// 한글 글리프는 unicode-range 분할 파일로 필요할 때만 받으므로 preload는 끈다.
const notoSansKr = Noto_Sans_KR({
  variable: "--font-noto-sans-kr",
  weight: ["400", "500", "700"],
  preload: false,
});

const jua = Jua({
  variable: "--font-jua",
  weight: "400",
  preload: false,
});

const nanumPen = Nanum_Pen_Script({
  variable: "--font-nanum-pen",
  weight: "400",
  preload: false,
});

// 책장 제목(학교안심 시간표)과 문구(그리운 나람) — Google Fonts에 없어 파일을 직접 포함한다.
const siganpyo = localFont({
  src: "./fonts/Hakgyoansim_SiganpyoOTFR.otf",
  variable: "--font-siganpyo",
  display: "swap",
  preload: false,
});

const griunNaram = localFont({
  src: "./fonts/Griun_Naram-Rg.ttf",
  variable: "--font-griun-naram",
  display: "swap",
  preload: false,
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="ko"
      className={`${geistSans.variable} ${geistMono.variable} ${notoSansKr.variable} ${jua.variable} ${nanumPen.variable} ${siganpyo.variable} ${griunNaram.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
