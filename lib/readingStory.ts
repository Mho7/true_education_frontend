// 번갈아 읽기에 쓰는 책 데이터. 쪽마다 문장 목록이 있고, 여울이와 아이가 한 문장씩 번갈아 읽는다
// (첫 문장은 여울이, 다음 문장은 아이 … 쪽이 바뀌어도 순서는 이어진다).

export type ReadingBook = {
  id: string;
  title: string;
  /** 쪽 → 문장 목록 */
  pages: string[][];
};

// 시안 문장으로 만든 예시 책
const SAMPLE_BOOK: ReadingBook = {
  id: "sample-little-bear",
  title: "작은 곰과 꿀단지",
  pages: [
    ["작은 곰은 숲속 길을 천천히 걸어갔어요.", "그러다 반짝이는 꿀단지를 발견했어요.", "작은 곰은 깜짝 놀라 눈을 크게 떴어요."],
    ["꿀단지 안에는 달콤한 꿀이 가득했어요.", "작은 곰은 친구들이 생각났어요.", "혼자 먹기에는 너무 많았거든요."],
    ["작은 곰은 토끼네 집으로 달려갔어요.", "토끼야, 같이 꿀을 먹자!", "토끼는 귀를 쫑긋 세우며 웃었어요."],
    ["둘은 다람쥐와 부엉이도 불렀어요.", "친구들이 모두 나무 아래 모였어요.", "꿀을 한 숟가락씩 나누어 먹었어요."],
    ["나누어 먹으니 꿀이 더 달콤했어요.", "작은 곰은 행복하게 웃었어요.", "오늘은 정말 멋진 하루였어요."],
  ],
};

/**
 * 오늘 번갈아 읽기로 읽을 책을 가져온다. 서버(페이지)에서 불러 화면에 넘긴다.
 * TODO: 책 내용 데이터(크롤링·백엔드 API)가 준비되면 여기서 받아 와 ReadingBook 모양으로 바꿔 돌려준다.
 *       문장 나누기가 안 된 본문이면 여기서 문장 단위로 잘라 쪽마다 넣는다.
 */
export async function getReadingBook(): Promise<ReadingBook> {
  return SAMPLE_BOOK;
}
